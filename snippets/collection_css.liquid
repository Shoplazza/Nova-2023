<style>
  .main-content{
    padding-bottom: 32px;
  }
  @media (min-width: {{settings.breakpoint}}px) {
    .main-content{
      padding-bottom: 44px;
    }
  }
  .collection_title {
    font-size: 24px;
    opacity: 0.85;
  }
  .collection-list {
    gap: 46px 40px;
  }
  .search_load_more {
    outline: 2px solid transparent;
    outline-offset: 2px;
    font-weight: 600;
    padding: 12px 22px;
  }
  .collection-list {
    display: grid;
    grid-template-columns: repeat({{columns_pc}},minmax(0, 1fr));
  }
  .click_pagination {
    grid-column-start: span {{columns_pc}};
  }
  .collection_filter_title {
    margin-right: 20px;
  }
  .filter-item {
    margin-right: 12px;
    border: 1px solid rgba(230,230,230);
    padding: 8px 12px;
    position: relative;
  }
  .filter-item span {
    padding-right: 20px;
  }
  .filter-content {
    background: #fff;
    border: 1px solid #e6e6e6;
    padding: 16px 20px;
  }
  .filter-item .i-spzhtml-lightbox-mask {
    background: transparent;
  }
  .filter-item-option {
    font-size: 16px;
    line-height: 20px;
    padding: 20px 0;
    border-bottom: 1px solid rgba(229,231,235);
  }

  .filter-item-option-checkbox {
    width: 20px;
    height: 20px;
    overflow: hidden;
    border-radius: 2px;
    margin-right: 24px;
  }

  .filter-item-option-checkbox svg {
    opacity: 0;
  }
  .filter-item-option[selected] .filter-item-option-checkbox svg {
    opacity: 1;
  }
  .filter_checkbox_svg {
  	color: var(--color-body-text);
  }
  .filter_item_total {
  	padding: 20px 0;
  	border-bottom: 1px solid rgba(229,231,235);
  }
  .filter-content-inner-render {
    height: auto !important;
  }
  .filter_price {
    padding: 20px 0;
    border-top: 1px solid rgba(229,231,235);
  }
  .filter-item-price-container {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 24px;
    padding-bottom: 24px;
  }
  .filter-item-price input {
    padding: 14px 16px;
  }
  .filter-item-price input::placeholder {
    font-size: 16px;
  }
  .filter_result_item {
    border: 1px solid rgba(229,231,235);
    border-radius:9999px;
    padding: 8px 12px;
    margin-bottom: 10px;
  }
  .filter-result-container[no-data] {
    display: none;
  }
  .filter-result-container {
    margin-bottom: 20px;
  }
  .filter_sort_select {
    margin-right: 0;
  }
  .collection_empty {
    padding: 116px 0;
  }
  .collection__filter_clear_all {
    box-shadow: 0 1px 0 0 #E6E6E6; 
    cursor: pointer;
  }
  .collection-list[nomore] div[role='loadmore']{
    display: none;
  }

  .collection-list[loading] ~ .loading {
    display: var(--loading-show);
  } 

  .filter-item spz-dropdown {
    z-index: var(--z-index-dropdown)!important;
  }

  @media (max-width: {{ settings.breakpoint }}px) {
    .collection-list {
      grid-template-columns: repeat({{columns_mobile}},minmax(0, 1fr));
      gap: 24px 15px;
    }
    .click_pagination {
      grid-column-start: span {{columns_mobile}};
    }
    .filter_mobile_btn {
      padding: 10px;
      border: 1px solid #e6e6e6;
      background: #fff;
      margin-bottom: 14px;
    }
    .filter-mobile-sidebar {
      background: #fff;
    }
    .filter-mobile-header {
      height: 60px;
      padding: 16px;
      border-bottom: 1px solid rgba(230,230,230);
    }
    .filter-mobile-close {
      position: absolute;
      right: 16px;
    }
    .filter-mobile-item-header {
      padding: 16px !important;
      border-bottom: 1px solid rgba(230,230,230);
    }
    .filter-mobile-content-inner-render {
      min-width: 1px !important;
    }
    .filter-mobile-content-inner-render spz-nested-menu [spz-nested-submenu] {
      padding: 16px !important;
    }
    .nest-submenu-header svg {
      transform: rotate(180deg);
      margin-left: -7px;
      margin-right: 6px;
    }
    .nest-submenu-header {
      padding: 16px 0;
      border-bottom: 1px solid rgba(230,230,230);
    }
    .filter-nested-submenu {
      padding: 16px;
      width: 100%;
      overflow-y: scroll;
    }
    .filter-nested-submenu .filter-item-option {
      padding: 16px 22px;
    }
    .filter-item-price input {
      margin-left: 6px;
    }
    .filter-mobile-footer {
      padding: 20px 16px 16px;
    }
    .filter-mobile-footer button {
      width: 50%;
      height: 52px;
    }
    .filter-mobile-footer button:first-child {
      margin-right: 20px;
    }
    .filter-mobile-content-inner-render {
      height: auto !important;
    }
    .collection_sort_container {
      padding: 40px 16px;
      justify-content: space-between;
      border-bottom: 1px solid rgba(230,230,230);
    }
    .filter_sort_select {
      border: none;
    }
    .filter_sort_select select {
      text-align: center;
    }
    #filter-sidebar {
      height: 75% !important;
      overflow: hidden !important;
    }

    #filter-menu-mobile:not([child-open]) .filter-menu-inner-container {
      overflow-y: auto;
      overflow-x: hidden;
    }

    .filter-menu-inner-container spz-render {
      height: auto !important;
      position: unset !important;
    }

    .filter-item-header {
      padding: 16px;
    }
    .filter-item-header {
      border-bottom: 1px solid #e6e6e6;
    }
  }

  @media (min-width: {{ settings.breakpoint }}px) {
    .collection_filter_container {
      width: {% if show_sort %}70%{% else %}100%{% endif %};
    }
    .collection_filter_title {
      padding-top: 12px;
    }
    .filter-item {
      margin-bottom: 10px;
      height: 40px;
    }
    .filter-content spz-selector {
      max-height: 432px;
      overflow-y: scroll;
    }
    .filter-item-header svg {
      transform: rotate(270deg);
    }
    .filter-item spz-dropdown[hidden] ~ .filter-item-header svg {
       transform: rotate(90deg);
    }
    .filter-content {
      width: 375px;
    }
  }
  {% if shop.locale == 'ar-SA' %}
    @media (min-width: {{ settings.breakpoint }}px) {
      .collection_filter_title {
          margin-right: 0;
          margin-left: 20px;
      }
    }
    .filter-mobile-footer button:first-child {
      margin-left: 20px;
      margin-right: 0;
    }
    .filter-item-header svg {
      transform: rotate(180deg);
    }
    .nest-submenu-header svg {
      margin-right: 0;
      margin-left: 6px;
      transform: rotate(0);
    }
    .filter-item-option-checkbox {
      margin-left: 24px;
      margin-right: 0;
    }
  {% endif %}
</style>


